version: "3.8"

services:
  postgres14:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5486:5432"
    volumes:
      - postgres14_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres15:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5485:5432"
    volumes:
      - postgres15_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres16:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5484:5432"
    volumes:
      - postgres16_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5487:5432"
    volumes:
      - postgres_test_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres-pgvector:
    image: pgvector/pgvector:0.8.1-pg17
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5488:5432"
    volumes:
      - postgres_pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres-postgis:
    image: imresamu/postgis:17-3.5-alpine
    environment:
      POSTGRES_DB: sql_terraform_test
      POSTGRES_USER: test_user
      POSTGRES_PASSWORD: test_password
    ports:
      - "5489:5432"
    volumes:
      - postgres_postgis_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U test_user -d sql_terraform_test"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres14_test_data:
  postgres15_test_data:
  postgres16_test_data:
  postgres_test_data:
  postgres_pgvector_data:
  postgres_postgis_data:
