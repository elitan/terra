# test matrix

## dimensions

- feature
- operation: create, alter, drop, idempotent-reapply, rollback-on-error
- dialect: postgres, sqlite
- postgres version: 14, 15, 16, 17, 18

## sqlite runtime baseline

- desired-schema parser, migration executor, and inspector: exact-pinned `libsql` 0.5.29 with SQLite 3.45.1
- the runtime contract verifies SQLite 3.45.1 plus the `ENABLE_FTS5` and `ENABLE_RTREE` compile features
- desired schemas are parsed by the execution runtime so accepted grammar cannot exceed apply support
- query-derived `CREATE TABLE ... AS SELECT`/`VALUES`/CTE forms fail before target mutation because their initial rows are not declarative schema state

## status key

- `covered`: has tests in repo
- `partial`: tests exist but missing one or more operations or version breadth
- `gap`: no test yet

PostgreSQL serial coverage recognizes catalog type, owned-sequence/default
dependencies, and the exact canonical `nextval` expression rather than matching
keyword text. Table/column
creation, whole-column addition/removal, external convergence, and idempotency
are covered. The documented `serial2`, `serial4`, and `serial8` aliases normalize
to `smallserial`, `serial`, and `bigserial` with implicit `NOT NULL`, and both
spellings converge. Existing-column transitions to/from serial or between serial
sizes reject before mutation and preserve columns, defaults, sequences, and rows.
Serial-owned sequence start, increment, minimum, maximum, cache, cycle, and
version-appropriate persistence must retain their canonical definitions; every
drift form rejects before mutation, while live counter advancement and `RESTART`
state remain unmanaged. Persistence matches the owning table on PostgreSQL 15+
and remains logged for PostgreSQL 14 unlogged tables.
Desired serial columns reject explicit `NULL`, `DEFAULT`, identity, and
generated clauses before planning because those conflict with serial's implicit
`NOT NULL` and sequence default; explicit `NOT NULL` remains equivalent and
idempotent. Parser and apply-level scenarios cover canonical and alias spellings,
pre-mutation failure, and non-application of preceding desired changes.
Serial pseudo-types with `pg_catalog` qualification, type modifiers, or array
bounds reject before planning instead of being normalized or deferred to server
execution. Quoted lowercase `"serial"` remains equivalent to serial, and other
schema-qualified names ending in `serial` remain on the custom-type path.
Catalog inspection combines `format_type` with `pg_type.typnamespace` for
custom types whose names collide with all six serial aliases. Scalar and array
table columns, partition column reconstruction, and composite attributes retain
their schema identity; live PostgreSQL 14/18 lifecycle tests preserve type/table
OIDs and rows and replan empty.

## current feature matrix

| feature | postgres | sqlite | notes |
|---|---|---|---|
| tables | covered | covered | PostgreSQL inheritance and ordinary unlogged-table persistence are lifecycle-tested; `NO INHERIT` detachment is destructive and strict-mode blocked while preserving parent-query visibility, inherited definitions, and child rows across PostgreSQL 14-18, while compatible `INHERIT` attachment remains safe; persistent table clustering choices are lifecycle-tested, with `SET WITHOUT CLUSTER` removal destructive and strict-mode blocked while preserving the selected index, table OID, and populated rows, whereas `CLUSTER ON` assignment remains safe; table storage-parameter `RESET` is emitted separately from compatible changes, classified as destructive, and strict-mode blocked while preserving the exact parameters, table OID, and populated rows, whereas storage-parameter `SET` remains safe; partition-bound changes use transactional native detach/attach without dropping the child, and `DETACH PARTITION` is destructive and strict-mode blocked while preserving child OID, original bound, partition linkage, parent-query visibility, and populated rows, whereas `ATTACH PARTITION` remains safe; logged-to-unlogged durability weakening is likewise destructive and strict-mode blocked with exact persistence and populated-row preservation, while restoring logged persistence remains safe; unlogged partitioned parents and explicit unlogged leaf partitions fail before planning in desired SQL and external PostgreSQL 14-17 catalog state because PostgreSQL 18 rejects unlogged parents and earlier releases do not propagate persistence consistently; structure-copying `LIKE` and typed `OF` shorthand also fail before planning rather than becoming empty/partial tables; ordinary and partition hierarchy removal drops managed triggers, ordinary/materialized views, foreign keys, policies, and leaves first, then uses `RESTRICT` so unmanaged views or foreign keys block and roll back the apply instead of being deleted or stripped; real constraint drops are routed ahead of partition removal from PostgreSQL AST nodes, while table creation and foreign-key additions that contain `DROP CONSTRAINT` in quoted identifiers remain after newly referenced partitions; SQLite regular/FTS5/RTree virtual tables, shadow filtering, STRICT/WITHOUT ROWID, exact DDL, quoted identifiers, ASCII case-folded table/column matching and recreation copies, hidden ROWID preservation including shadow-name and inline `PRIMARY KEY DESC` semantics, pre-mutation rejection when all ROWID names are inaccessible, collision-safe recreation names across tables and indexes, conflict-policy-safe row transfer that aborts instead of applying schema `IGNORE`/`REPLACE`, coordinated foreign-key table drops, migration-time foreign-key and database-integrity verification with `foreign_keys` and `defer_foreign_keys` restoration, migration-time CHECK enforcement with `ignore_check_constraints` restoration, forced schema validation with `writable_schema` restoration, active transaction/savepoint preflight, AUTOINCREMENT high-water preservation, changes, removal, and rollback covered in memory and on disk |
| columns | covered | covered | PostgreSQL `numeric`/`decimal` precision `1..1000`, PostgreSQL 14 scale `0..precision`, and PostgreSQL 15-18 scale `-1000..1000` are version-validated before mutation across ordinary and partitioned columns, arrays, composite attributes, and domain bases; extended-scale values are stored and reapplied with row preservation, and uninspectable numeric range-subtype modifiers fail before planning; PostgreSQL `time`, `timestamp`, and `interval` precision `0..6` is validated before the server can silently clamp it, zero typmods remain explicit, all 13 documented interval field masks round-trip across ordinary and partitioned columns, bounded arrays, composite attributes, and domain bases, and uninspectable temporal range-subtype modifiers fail before planning; PostgreSQL character lengths `1..10485760` and bit-string lengths `1..83886080` are validated across the same object families, constrained range subtypes fail before planning, unbounded `bpchar`/`varchar`/`varbit` remain distinct, and the internal one-byte `"char"` identity round-trips across columns, arrays, composites, domains, ranges, functions, and procedures with OID and row preservation; PostgreSQL SQL-standard `float` precision aliases are lifecycle-tested at the documented `1`/`24` real and `25`/`53` double-precision boundaries plus omitted precision across columns, bounded arrays, composite attributes, domain bases, and range subtypes, with external convergence, OID/row preservation, idempotent reapply, and pre-mutation rejection outside `1` through `53`; PostgreSQL decorative array size bounds, repeated dimensions, and SQL-standard `ARRAY[n]` declarations normalize to the element's one catalog array type across builtin/custom ordinary and partition columns, composite attributes, domain bases, range subtypes, and routines without masking scalar/array changes; PostgreSQL per-column statistics targets plus `n_distinct` and `n_distinct_inherited` overrides are parsed, catalog-inspected across the 14-16 negative and 17-18 null default representations, created, changed, reset, externally converged, inheritance-localized, type/rename-preserved, rollback-tested, and idempotently reapplied for ordinary/inherited tables and materialized views; generated statistics-target `-1` and distinct-option `RESET` statements are explicit, destructive, and strict-mode blocked while preserving exact planner metadata, relation/index OIDs, and populated rows, whereas positive targets and overrides remain safe; invalid ranges, duplicate/unknown options, missing columns, implicit materialized-view names, and partition drift fail before mutation. PostgreSQL `SET COMPRESSION default` omission-driven resets are emitted separately from compatible storage changes, classified as destructive, and strict-mode blocked while preserving the exact physical catalog state, table OID, and populated rows across PostgreSQL 14-18; ordinary reset and strict-safe concrete compression assignment both converge without rewriting existing rows. PostgreSQL identity generation changes are lifecycle-tested on 14-18: weakening `GENERATED ALWAYS` to `BY DEFAULT` is emitted separately from compatible sequence-option changes, classified as destructive, and strict-mode blocked while preserving table/sequence OIDs, live sequence state, and populated rows; ordinary apply demonstrates explicit-value acceptance, while restoring `ALWAYS` remains safe and restores explicit-value rejection. PostgreSQL stored generated columns are lifecycle-tested on 14-18; PostgreSQL 18 implicit and explicit VIRTUAL generated columns are parsed, created, inspected, evaluated, changed to STORED with base-row preservation, and reapplied idempotently, while 14-17 reject them before mutation; SQLite preserves case-normalized declared type names including `INT`, `INTEGER`, parameterized text, empty, unusual affinity, and `ANY` forms; ordinary numeric-affinity versus `STRICT` storage-preserving `ANY` behavior, STRICT coercion failure, inspection, recreation, row preservation, and idempotency are runtime-tested; VIRTUAL/STORED inspection, ASCII case-folded generated expressions, add, recreate, remove, rollback, and memory/file parity are covered |
| constraints | covered | covered | PostgreSQL foreign-key MATCH FULL and NOT VALID state, omitted referenced-column inference, and CHECK NO INHERIT/NOT VALID state are lifecycle-tested on 14-18, including validation rollback and inheritance enforcement; all 40 unequal `NO ACTION`/`RESTRICT`/`CASCADE`/`SET NULL`/`SET DEFAULT` transitions across `ON DELETE` and `ON UPDATE` are generated and verified on 14-18 for planned replacement, existing-row preservation, exact catalog convergence, empty replan, and runtime behavior; generated immutable row-local comparison and `BETWEEN` CHECK expressions are property-tested for apply/replan idempotency on the supported version endpoints; PostgreSQL 18 column `NOT NULL` catalog rows are normalized through column nullability rather than duplicated as table constraints, while `NOT ENFORCED`, temporal `WITHOUT OVERLAPS`/`PERIOD`, and advanced named/table-level/inheritance/validation `NOT NULL` forms fail before mutation in desired SQL; unenforced, temporal, and `NOT NULL` inheritance/validation flags also fail in managed external catalog state, while ordinary external `NOT NULL` names normalize to column nullability; implicit references resolve forward, composite, self, quoted, and schema-qualified primary keys while unresolvable external shorthand fails before planning; PostgreSQL 15+ ON DELETE column subsets are preserved while 14 rejects them before planning; MATCH SIMPLE normalizes to the default, failed validation rolls back, and unimplemented MATCH PARTIAL is rejected before planning; SQLite named inline/table constraints, collations, sort order, ON CONFLICT behavior, ASCII identifier and CHECK-expression folding, omitted single/composite parent primary-key targets, and explicit parent-key column, uniqueness, partial-index, and collation validity are preserved or normalized; ordinary rowid-table primary keys retain SQLite's historical nullability while exact `INTEGER PRIMARY KEY` ROWID aliases, explicit `NOT NULL`, `STRICT`, and `WITHOUT ROWID` keys are non-null, with inline versus table-level `DESC` alias behavior and NULL-key preservation through recreation covered; bidirectional `INT`/`INTEGER` key transitions preserve named and hidden row identity, while nullable-to-ROWID-alias promotion uses a collision-safe guard that rolls back on existing NULL keys without artifacts or row mutation; MATCH SIMPLE normalizes to SQLite's default while unenforced MATCH modes are rejected before planning; deferred foreign-key timing is recovered from stored SQL, lifecycle- and behavior-tested, and distinguished from all equivalent immediate spellings without relying on pragma row order |
| indexes | covered | covered | PostgreSQL keys are preserved as one ordered sequence across mixed column/expression and multiple-expression indexes, including every per-key collation, effective operator class and options, ASC/DESC and NULLS ordering, and expression statistics target; creation, inspection, targeted statistics changes/resets, external convergence, replacement rollback, concurrent ordering, and idempotent table/materialized-view reapply are lifecycle-tested on 14-18. Expression-index statistics-target `-1` resets are destructive and strict-mode blocked without replacing the index, while positive targets remain safe. Generated immutable Boolean/text/numeric partial predicates and `LOWER`/`UPPER`/`TRIM`/`LENGTH` expression keys are property-tested for apply/replan idempotency on the supported version endpoints. Catalog dependency columns include keys, payloads, expressions, and predicates so column removal suppresses redundant index drops and can recreate a same-named replacement; same-name tables and indexes remain isolated across schemas, while indexes targeting undefined or ambiguously unqualified non-public tables fail before planning. SQLite mixed column/expression keys, ASCII case-folded names, terms, expressions, and partial predicates, collations, ASC/DESC ordering, planner use, recreation, rollback, and memory/file parity are covered. |
| replica identity | covered | gap | PostgreSQL DEFAULT/FULL/NOTHING/USING INDEX state is parsed, catalog-inspected through `relreplident`/`indisreplident`, created, altered, removed, externally converged, and idempotently reapplied on ordinary tables, partitioned parents, and leaves. Explicit `NOTHING` is destructive and strict-mode blocked while preserving full old-row identity and populated rows across PostgreSQL 14-18; `FULL`, `DEFAULT`, and `USING INDEX` remain safe so identity strengthening and temporary replacement resets can proceed. Selected standalone and constraint-backed indexes cover quoted names, nullable INCLUDE payloads, constraint rename, physical replacement, concurrent-build ordering, and externally dropped index repair. Invalid non-unique, nullable-key, partial, expression, missing, or deferrable targets fail before mutation. Constraint-backed changes stay transactional; assignments dependent on concurrent standalone builds run afterward. SQLite unsupported by design. |
| views | covered | covered | PostgreSQL ordinary and materialized view query comparison uses parsed syntax trees: local-schema and redundant single-source column qualification, lowercase identifier quoting, comments, formatting, and redundant parentheses normalize structurally while string, escape-string, and dollar-quoted literal values remain exact; lifecycle coverage proves whitespace plus schema- and source-looking literal changes replace an ordinary view, change runtime output, preserve base rows, and reapply idempotently across PostgreSQL 14-18. PostgreSQL materialized-view output-column statistics targets and distinct-count overrides are preserved through explicit-name parsing, catalog inspection, native column renames, view recreation, external convergence, reset, rollback, and idempotent reapply; position-1 single-expression index targets are included in the same lifecycle. Their generated planner-metadata resets are explicit, destructive, and strict-mode blocked while preserving exact settings, relation/index OIDs, and materialized rows. Persistent materialized-view clustering choices are lifecycle-tested, with `SET WITHOUT CLUSTER` removal destructive and strict-mode blocked while preserving the selected index, relation OID, and materialized rows, whereas `CLUSTER ON` assignment remains safe. PostgreSQL ordinary-view `check_option`, `security_barrier`, and PostgreSQL 15+ `security_invoker` options are parsed in every supported form, inspected, generated, behavior-tested, changed/reset with native `ALTER VIEW` without losing OIDs or dependents, rolled back, externally converged, and rejected before mutation on unsupported versions or values. Removing any enforcement option, changing `CASCADED` checks to `LOCAL`, or setting either security option false is destructive and strict-mode blocked while preserving the exact query, options, OID, and base rows; additions, `LOCAL` to `CASCADED`, and true-valued security changes remain safe. Simultaneous query and option changes preserve the current enforcement through `CREATE OR REPLACE` before applying an explicit option transition. Ordinary and materialized view output-column lists are parsed, inspected, generated, renamed collision-safely without losing dependents or materialized rows, expanded/reduced, rolled back, and reapplied idempotently; materialized-view indexes preserve complete metadata through creation, inspection, external convergence, targeted replacement/removal, view recreation, schema isolation, and idempotent reapply; materialized-view population state, access methods, storage parameters, and tablespaces survive the full lifecycle, alter in place where the server supports it, fail before mutation on unsupported PostgreSQL versions, roll back transactionally, and converge idempotently; materialized-view storage-parameter `RESET` is destructive and strict-mode blocked while retaining the exact parameters, tablespace, relation/index OIDs, materialized data, and dependent views, whereas parameter `SET` remains safe; ordinary-view index targets fail before planning; SQLite full definitions, effective and explicit output-column inspection, ASCII case-folded object/definition matching, replacement, recreation ordering, and rollback covered |
| schemas | covered | gap | PostgreSQL explicit and authorization-derived schema names plus concrete named owners are parsed, created after same-apply role creation, catalog-inspected, altered in place after external drift, quoted, idempotently reapplied, and lifecycle-tested on 14-18. Existing-schema `OWNER TO` transfers are destructive and strict-mode blocked while preserving the former owner, schema OID, contained table, and populated rows; creation with inline `AUTHORIZATION` remains safe. `IF NOT EXISTS` normalizes to declarative existence. Session-context owners, inline schema elements, and duplicate declarations fail before mutation instead of losing owner or contained-object state; SQLite unsupported by design. |
| roles | covered | gap | PostgreSQL `ROLE`, `USER`, and legacy `GROUP` creation forms normalize to one cluster-wide role identity with complete login, superuser, database/role creation, inheritance, replication, row-level-security bypass, and connection-limit state. Creation, external inspection, every attribute alteration through native `ALTER ROLE`, quoted names, alias/default equivalence, drift repair, deterministic ordering, OID/ownership/membership/password/configuration preservation, explicit destructive removal, strict-mode blocking, dependency rollback, unrelated-global filtering, and idempotent reapply are covered across PostgreSQL 14-18. Explicit `NOLOGIN`, `NOSUPERUSER`, `NOCREATEDB`, `NOCREATEROLE`, `NOINHERIT`, `NOREPLICATION`, and `NOBYPASSRLS` capability removal is destructive and strict-mode blocked while retaining the exact role attributes, OID, external schema ownership, membership, password/expiration, and configuration; positive capability grants remain safe. Replacing a finite connection limit with PostgreSQL's `-1` unlimited default is likewise destructive and strict-mode blocked while preserving the role OID and finite catalog value, whereas assigning or restoring a finite limit remains safe and converges in place. Password/expiration, inline membership, ignored `SYSID`, duplicate attributes/declarations, contextual or imperative alterations, and invalid limits fail before mutation. SQLite unsupported by design. |
| object privileges | covered | gap | PostgreSQL concrete table, sequence, schema, and foreign-server grants are expanded from combined privilege/object/grantee lists into atomic stable identities, parsed and catalog-inspected through ACL expansion, created, repaired after external drift, removed by omission with destructive `REVOKE ... RESTRICT`, strict-mode blocked, and idempotently reapplied across PostgreSQL 14-18. `WITH GRANT OPTION` upgrades and downgrades use native grant/revoke-option statements without removing access. Implicit owner/default ACL entries are retained without spurious revokes, pseudo-`PUBLIC` remains distinct from a quoted role named `PUBLIC`, foreign-server grants require a declared server and follow its ownership, and PostgreSQL 17-18 `MAINTAIN` plus other unsupported ACLs remain unmanaged. Direct `REVOKE`, role membership, columns, `ALL`, expanding schema targets, unsupported object families, contextual grantees, explicit grantors, duplicate atomic declarations, unbound global targets, and unsafe non-owner grantor provenance fail before mutation. SQLite unsupported by design. |
| default privileges | covered | gap | PostgreSQL global and per-schema defaults for tables, sequences, routines, types, and schemas are expanded across owners, schemas, privileges, and grantees into stable atomic state. Explicit positive grants, negative hard-wired defaults, `PUBLIC`, grant-option transitions, external drift, external convergence, future-object behavior, omission back to global/per-schema baselines, strict-mode blocking, transactional rollback, and same-apply ordering are parser-, catalog-, planner-, and lifecycle-tested across PostgreSQL 14-18. Managed owners require a declared role and per-schema defaults require a declared schema. PostgreSQL's `pg_default_acl` sequence code is translated to the distinct `acldefault` sequence code before baseline comparison. `ALL`, contextual roles, `CASCADE`, grant-option-only revocations, nested schema targets, PostgreSQL 17-18 `MAINTAIN`, PostgreSQL 18 large objects, unsupported families, and unsafe grantor provenance fail early or remain preserved unmanaged. SQLite unsupported by design. |
| object comments | covered | gap | PostgreSQL schema, ordinary/partitioned table, relation/composite column, ordinary/materialized view, ordinary index, sequence, and enum/composite/domain/range/multirange type comments are parsed, catalog-inspected with `objoid` plus `classoid`, created, updated, removed by omission, schema-scoped, snapshot-tested on 14-18, and reapplied idempotently. Generated `COMMENT ON ... IS NULL` removals are destructive and strict-mode blocked while preserving every stored supported-target comment; storing or replacing a non-empty comment remains safe. `DOMAIN` normalizes to the equivalent `TYPE` identity. Explicit NULL/empty removal, duplicate final declarations, and every target whose full identity is not modeled fail before mutation instead of disappearing from desired state; SQLite unsupported by design. |
| row-level security and policies | covered | gap | PostgreSQL ENABLE/FORCE flags and complete CREATE POLICY state are parsed, catalog-inspected, behavior-tested as a non-owner, changed, removed, externally converged, and reapplied idempotently on 14-18; every ALL/SELECT/INSERT/UPDATE/DELETE command, permissive/restrictive mode, PUBLIC/named/contextual roles, USING/WITH CHECK expressions, implicit checks, quoted/Unicode identifiers, string-literal whitespace changes, combined RLS clauses, and supported mixed ALTER TABLE constraints are covered; DISABLE and NO FORCE plans are classified as destructive and blocked by strict mode while retaining the policy and exact enforcement state; a later policy-creation failure transactionally rolls back earlier policy replacement plus ENABLE/FORCE removal, preserving catalog state, protected-row visibility, and stored data; ALTER POLICY and negative RLS mutations fail before database changes because absence declares removal; SQLite unsupported by design |
| enums | covered | gap | PostgreSQL zero-label enums and empty, quoted, backslash, and Unicode labels are preserved; insertion-only evolution supports labels at any position, commits additions before transactional DDL that may use them, and converges idempotently; removal or reordering of existing labels fails before mutation; direct/array relation attributes, composites, derived domains/ranges, routine signatures, defaults, constraints, indexes, and other catalog dependencies are inspected, retained managed or unmanaged dependents fail during planning with exact identities, coordinated column/routine/type removal is ordered, and unmodeled dependents such as casts are rejected before mutation; SQLite unsupported by design |
| composite types | covered | gap | PostgreSQL zero-attribute definitions, quoted identifiers, schema-qualified nested types, arrays, typemods, explicit collations, dependency-ordered create/drop, positional and combined rename/append, type/collation change, destructive drop, row preservation, rollback, external creation, cross-schema dependent protection, and idempotent reapply are covered on 14-18; explicit visible-schema custom type references retain catalog namespace identity across scalar/array table and partition columns, composite attributes, domain bases, and range subtypes without masking schema or shape drift; direct, array, domain, range, multirange, composite, index, view, routine-signature, policy, trigger, cast, relation-expression, and unmanaged dependencies are catalog-traversed with machine-readable identities, retained dependents block planning, and modeled removals are coordinated; enum/composite/domain/range creation and composite alteration use one cross-family graph, with reverse removal ordering and pre-mutation cycle/ambiguity/name-collision rejection; middle insertion and retained-attribute reordering fail before mutation; SQLite unsupported by design |
| domain and range types | covered | gap | PostgreSQL domains preserve base type, typmod/array shape, collation, defaults, nullability, named/generated checks, and validation state; native default/nullability/constraint alteration, constraint rename/validation, duplicate checks, row preservation, failed-validation rollback, guarded unused base/collation replacement, and idempotent external convergence are lifecycle-tested on 14-18. Domain `DROP DEFAULT` and `DROP NOT NULL` weakening is destructive and strict-mode blocked while preserving the domain/table OIDs, catalog state, stored rows, inherited default behavior, and null rejection; `SET DEFAULT` and `SET NOT NULL` remain safe. PostgreSQL's inability to add/validate a constraint or set `NOT NULL` when the domain or a derived domain is nested in an array, composite, or range table column is detected recursively and rejected during planning. Safe default changes remain available for container domains and are classified from semantic operations, so validation keywords inside quoted domain names cannot cause false rejection. Ranges preserve subtype, default/non-default opclass semantics, collation, canonical/subtype-difference references, and automatic/explicit multirange names; immutable option changes use guarded drop-before-create replacement only without dependents. Direct, array, multirange, derived domain/range, relation, routine-signature, policy, trigger, cast, relation-expression, and unmanaged dependencies are inspected with machine-readable identities; retained dependencies fail replacement/removal during planning, modeled policy/trigger removal is coordinated, and every domain/range drop uses `RESTRICT`. Same-apply creation and removal share the enum/composite/domain/range dependency graph, including generated multirange aliases; cycles, ambiguous references, and physical name collisions fail before mutation. Same-apply creation of a range support function is rejected before mutation because PostgreSQL requires the function (and canonical shell-type workflow) first; built-in and already-existing support functions are supported. SQLite unsupported by design |
| sequences | covered | gap | PostgreSQL standalone and identity-backed sequences preserve type, bounds, start, increment, cache, cycling, ownership, and logged/unlogged persistence; native in-place alteration preserves relation/sequence OIDs, dependents, rows, and live counter state across definition, ownership, and persistence changes. Sequence creation and ownership detachment are structurally planned before table changes, while ownership attachment and standalone removal follow dependent table changes; this preserves retained sequences when owner tables are removed, removes defaults before referenced sequences, and cannot be confused by quoted identifiers containing `OWNED BY`. Parser `CAST(... AS regclass)` and catalog `::regclass` defaults converge for quoted sequence names. Standalone numeric type changes emit `AS` only when the type actually differs, remain combined with required compatible bounds, are classified as destructive, and are strict-mode blocked while preserving the sequence OID, exact definition, and live value across PostgreSQL 14-18; ordinary apply narrows and widens in place and converges, while bound/start/increment/cache-only changes no longer carry a redundant destructive type clause. Enabling `CYCLE` wraparound is emitted separately from compatible changes, classified as destructive, and strict-mode blocked while retaining exact bounds, options, OIDs, rows, and live values across PostgreSQL 14-18; ordinary apply demonstrates boundary reuse, whereas `NO CYCLE` remains safe and restores exhaustion errors. Explicit ownership detachment, including the first phase of a target transfer, is destructive and strict-mode blocked while retaining the exact owner target, OID, live value, and called state across PostgreSQL 14-18; attaching an unowned sequence to `table.column` remains safe. PostgreSQL 15-18 support standalone `UNLOGGED` and explicit identity-sequence `LOGGED`/`UNLOGGED`, including external drift and table-persistence ordering; logged-to-unlogged durability weakening is destructive and strict-mode blocked while preserving the sequence OID, logged state, and live counter, while restoring logged persistence remains safe. PostgreSQL 14 rejects explicit persistence before mutation and retains its version-specific logged identity sequence behind unlogged tables. Quoted/Unicode names, schema-qualified ownership, removal, external creation, and idempotent reapply are covered; SQLite unsupported by design |
| functions | covered | gap | PostgreSQL empty and nonempty quoted bodies, search-path-independent custom/array type identities, externally created multi-column `RETURNS TABLE`, decorative array dimensions, `LEAKPROOF`, language-dependent `COST` defaults, and deterministic per-function `SET` configuration are parsed, inspected, behavior-tested, normalized, changed/reset with dependency-safe replacement, externally converged, rolled back, and reapplied idempotently; declared unqualified custom types are deterministically schema-qualified while ambiguous cross-schema references fail before mutation; safe input-parameter name additions preserve OIDs and dependents, routine drops use `RESTRICT`, and dependency-breaking recreation/removal fails during planning with exact dependent objects; creation-time column `%TYPE` references, environment-capturing `SET FROM CURRENT`, SQL-standard bodies, every C linked-object body form, `SUPPORT`, `WINDOW`, `TRANSFORM`, and aggregates fail before planning both in desired SQL and when detectable externally while unmodeled; SQLite unsupported by design |
| procedures | covered | gap | PostgreSQL empty and nonempty quoted bodies, search-path-independent custom/array type identities, decorative array dimensions, and deterministic per-procedure `SET` configuration are parsed, inspected, behavior-tested, normalized, changed/reset with OID-preserving replacement, externally converged, rolled back, and reapplied idempotently; declared unqualified custom types are deterministically schema-qualified while ambiguous cross-schema references fail before mutation; safe input-parameter name additions preserve OIDs, drops use `RESTRICT`, and dependency-breaking recreation/removal fails during planning; creation-time column `%TYPE` references, environment-capturing, SQL-standard-body, every C linked-object body form, transform, and other unmodeled routine forms fail before planning both in desired SQL and when detectable externally; SQLite unsupported by design |
| triggers | covered | covered | PostgreSQL ordinary, constraint, and event triggers preserve `UPDATE OF` columns, transition-table names, WHEN/argument semantics, function qualification, and origin/disabled/replica/always firing modes across create, external inspection, native mode alteration, runtime behavior, and idempotent reapply on 14-18; SQL-object canonicalization normalizes formatting, comments, and structural `EXECUTE PROCEDURE` syntax only outside lexical tokens, preserving exact quoted trigger-argument whitespace and keyword text; table/event `DISABLE` and `ENABLE REPLICA` enforcement weakening is destructive and strict-mode blocked while retaining origin catalog modes plus actual row- and DDL-trigger execution, while `ENABLE` and `ENABLE ALWAYS` remain safe; partition clones are excluded as implementation-owned only while their firing mode matches the parent, divergent clone state and desired `ALTER TABLE ONLY` fail before mutation, and bulk ALL/USER mutations are rejected; SQLite header metadata, ASCII case-folded object/definition matching, literal-safe and comment-normalized bodies, quoted-name drops, table/view recreation restoration, execution, rollback, and repeated reapply tested in memory and on disk |
| materialized views | covered | gap | PostgreSQL explicit populated/unpopulated state is parsed, catalog-inspected, changed natively, rollback-tested, and idempotently reapplied across 14-18. `REFRESH MATERIALIZED VIEW ... WITH NO DATA` depopulation is destructive and strict-mode blocked while preserving scannability, relation OID, and exact materialized rows; ordinary and explicit `WITH DATA` refresh remains safe and repopulates without rebuilding the view. SQLite unsupported by design. |
| extensions | covered | gap | PostgreSQL database-wide identity, version/schema changes, recursive `CASCADE` requirements, dependency-ordered `RESTRICT` removal, unmanaged-dependent rollback, global relocation without touching unrelated extensions, parser ambiguity rejection, and idempotent reapply are covered; the core dependency lifecycle runs on PostgreSQL 14-18, with pgvector/postgis suites retained for third-party extension coverage; SQLite unsupported by design. |
| foreign servers | covered | gap | PostgreSQL foreign servers are tracked by database-wide name with complete type, version, wrapper, sorted option state, and optional explicitly declared concrete ownership. Creation, external inspection, `IF NOT EXISTS` normalization, quoted/empty values, add/set/drop option changes, version change/removal, owner creation/drift repair, deterministic ordering, OID/user-mapping/foreign-table/grant preservation, explicit destructive `RESTRICT` removal, strict-mode blocking, dependent rollback, duplicate/contextual/unbound-owner rejection, immutable-change safety, and idempotent reapply are covered across PostgreSQL 14-18. Existing-server `OWNER TO` transfers are destructive and strict-mode blocked while retaining the prior owner, OID, user mappings, foreign tables, and grants; same-plan ownership initialization for a newly created server remains safe through exact quoted-name correlation. Standalone `VERSION NULL` and explicit option `DROP` removal are likewise destructive and independently strict-mode blocked while retaining exact version/options and dependents; concrete version replacement plus option `ADD`/`SET` and literal values containing removal keywords remain safe. Omitted ownership and unrelated global servers remain unmanaged. PostgreSQL cannot alter a server's type or wrapper in place, so those changes fail before mutation; rename and unsafe `CASCADE` syntax are rejected before mutation. SQLite unsupported by design. |
| advisory locks | covered | gap | sqlite unsupported by design |
| cli contract | partial | partial | JSON schema versioning, stable error codes/shapes, no-color output, phase counts, ordered statement metadata, strict mode, and successful/error exit paths are covered; successful PostgreSQL and SQLite `plan` and real `apply` JSON are byte-snapshotted from a pending create through an empty convergence re-plan, including the `dryRun: false` and `strict: false` fields; PostgreSQL and SQLite unique-index statements retain the `index` category across ordinary, conditional, and concurrent grammar forms, while PostgreSQL ADD/ALTER/VALIDATE/DROP/RENAME table-constraint actions retain the `constraint` category without misclassifying CREATE TABLE or constrained-column additions; PostgreSQL ALTER COLUMN type conversions and removal actions, column `SET COMPRESSION default`, domain `DROP DEFAULT`/`DROP NOT NULL`, materialized-view `WITH NO DATA` refresh, standalone sequence `AS` type changes, table/sequence `SET UNLOGGED`, sequence `OWNED BY NONE`, standalone/identity-sequence `CYCLE`, identity `SET GENERATED BY DEFAULT` weakening, `COMMENT ON ... IS NULL`, table/materialized-view `SET WITHOUT CLUSTER`, table/materialized-view storage-parameter `RESET`, table/materialized-view distinct-option `RESET`, table/materialized-view/index statistics-target `-1`, ordinary-view enforcement-option `RESET` or security `false`, table/event trigger `DISABLE` or `ENABLE REPLICA`, `REPLICA IDENTITY NOTHING`, table `NO INHERIT`, partition `DETACH`, existing-schema or existing-server `OWNER TO`, foreign-server `VERSION NULL` or option `DROP`, explicit role capability-removal options, and role `CONNECTION LIMIT -1` resets remain destructive, while column/domain default and NOT NULL additions, concrete column storage/compression assignments, identity `SET GENERATED ALWAYS` strengthening, materialized-view `WITH DATA` refresh, sequence parameter changes without a type change, storage-parameter `SET` changes, positive statistics targets and distinct overrides, sequence `NO CYCLE`, `SET LOGGED`, initial sequence ownership attachment, non-empty comments, `CLUSTER ON`, view-option additions/strengthening, trigger `ENABLE`/`ENABLE ALWAYS`, other replica-identity modes, `INHERIT`, partition `ATTACH`, owned-schema creation, newly created server ownership initialization, concrete foreign-server versions, option `ADD`/`SET`, positive role capability grants, and finite role connection limits stay strict-mode safe; broader category coverage remains open |
| deterministic output | covered | covered | JSON plan and real apply outputs are byte-snapshotted across pending and converged SQLite/PostgreSQL lifecycles; error snapshots, stable statement ordering, and lexical-safe category/risk metadata are also covered |
| parser robustness | partial | covered | malformed corpus and random ASCII fuzz plus deterministic SQLite grammar generation; PostgreSQL rejects session-local or query-derived tables plus all other untracked top-level data/query/session/transaction/maintenance/DDL commands before planning while preserving materialized views and routine-body SQL; PostgreSQL also exhaustively rejects every documented `COMMENT ON` target outside its lossless subset, removal forms, and duplicate declarations; PostgreSQL expression comparison normalizes only syntax-tree-equivalent formatting while retaining whitespace inside ordinary, escape, dollar-quoted, and identifier literals, fails closed on unequal unparsable text, and lifecycle-tests domain defaults/checks, table checks, generated columns, expression/partial indexes, policies, rollback, OID/row preservation, and convergence across PostgreSQL 14-18; SQLite enforces unconditional CREATE-only top-level input, preserves trigger-body DML, rejects temporary objects, query-derived tables, and `IF NOT EXISTS` on every managed object, and preflights external-file statements without keyword false positives; CTAS and conditional-create rejection cover comments, quoting, literals, trigger bodies, file/shared-memory targets, and target-state preservation. |
| performance budgets | partial | partial | perf tests exist, gate script pending |
| flake rerun gate | partial | partial | script/workflow added, trend tracking pending |

PostgreSQL table and materialized-view `SET ACCESS METHOD` changes are physical
rewrites on supported server versions and are classified as destructive. Strict
mode rejects both concrete-method changes and resets while preserving relation
OIDs, relfilenodes, populated rows, and materialized-view dependents; ordinary
apply performs the native rewrite and converges. PostgreSQL 14 rejects existing
relation access-method changes before mutation because it lacks this syntax.
Partitioned parents remain intentionally basic-only: PostgreSQL 14-16 reject a
parent access method, while PostgreSQL 17-18 retain it and use it for future
partitions. TerraDB rejects both desired definitions and retained external
parent methods before planning instead of erasing the inheritance distinction.
PostgreSQL table and materialized-view `SET TABLESPACE` changes move physical
data files and are classified as destructive, including moves embedded in a
batched table alteration. Strict mode preserves OIDs, current placement,
storage parameters, rows, and dependents; ordinary moves and default resets
then converge across PostgreSQL 14-18.
CLI statement metadata retains managed-object categories for native PostgreSQL
index, ordinary/materialized view, schema, extension, and trigger alterations,
including materialized-view refreshes, table-hosted trigger-mode changes, and
constraint/event-trigger creation and removal, instead of degrading them to
`other`, generic `table`, or `constraint` categories.
View, function, and procedure replacements emitted with PostgreSQL's
`CREATE OR REPLACE` syntax likewise retain their managed-object categories in
ordered CLI plan metadata.
Generated PostgreSQL `CREATE UNLOGGED TABLE` statements on PostgreSQL 14-18
and version-gated `CREATE UNLOGGED SEQUENCE` statements on PostgreSQL 15-18
are reported as `table` and `sequence`, respectively, rather than `other`.
SQLite FTS5/RTree `CREATE VIRTUAL TABLE` statements are reported as `table` in
text/JSON plan metadata, matching their ordinary `DROP TABLE` removal category.
CLI category and destructive-risk classification follows SQL lexical structure:
quoted strings/identifiers, PostgreSQL dollar bodies, SQLite bracket/backtick
identifiers, line comments, and nested block comments cannot inject keywords
such as `DROP CONSTRAINT` or `SET UNLOGGED` into metadata or strict-mode checks.
PostgreSQL planner channel routing recognizes only structural
`CREATE [UNIQUE] INDEX CONCURRENTLY` and `DROP INDEX CONCURRENTLY` commands;
defaults, index predicates, and identifiers containing `CONCURRENTLY` remain
inside the transactional migration instead of losing atomicity.

## version matrix target

| lane | postgres versions | sqlite |
|---|---|---|
| pr | 14, 18 | yes |
| nightly | 14, 15, 16, 17, 18 | yes |
| release | 14, 18 + extension suites | yes |

## explicit gaps backlog

1. add per-feature rollback-on-error assertions for all core objects
